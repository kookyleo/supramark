# Fixtures intentionally skipped by the reference generator.
#
# Format:
#   <relative-path-under-tests/>  TAB  <reason>
#
# Lines starting with `#` and blank lines are ignored.
# Rust-side byte-exact tests must also skip these — they have no
# paired tests/reference/.svg and never will, until the underlying
# cause changes upstream.

ext_fixtures/cypress/flowchart/46.mmd	MVP scope: `flowchart-elk LR` requires the opt-in @mermaid-js/layout-elk backend. See FEATURES.md (Non-Goals). Revisit if/when ELK support lands.
ext_fixtures/demos/class/08.mmd	Upstream-bug: the fixture text is lifted verbatim from mermaid-js/mermaid@11.14.0 `demos/classchart.html` (block starting `class Person { ... } class People List~List~Person~~`). Mermaid's own jison classDiagram parser rejects the final `class People List~List~Person~~` line with `Parse error on line 11 ... got 'ALPHA'` — the syntax appears in the upstream demo but is not accepted by the parser shipped in the same release. Remove from this list once upstream fixes either the demo or the grammar.
ext_fixtures/cypress/flowchart/116.mmd	Icon shapes (@{ icon: "aws:..." }) require upstream icon SVG registry; byte-exact parity needs porting ~500 icon paths.
ext_fixtures/cypress/flowchart/117.mmd	Icon shapes (@{ icon: "aws:..." }) require upstream icon SVG registry; byte-exact parity needs porting ~500 icon paths.
ext_fixtures/cypress/flowchart/118.mmd	Icon shapes (@{ icon: "aws:..." }) require upstream icon SVG registry; byte-exact parity needs porting ~500 icon paths.
ext_fixtures/cypress/flowchart/80.mmd	Stadium shape uses roughjs for path generation even with roughness=0; byte-exact parity requires porting roughjs rendering algorithm.
ext_fixtures/cypress/flowchart/131.mmd	Stadium shape uses roughjs for path generation even with roughness=0; byte-exact parity requires porting roughjs rendering algorithm.
ext_fixtures/cypress/flowchart/213.mmd	Stadium shape uses roughjs for path generation even with roughness=0; byte-exact parity requires porting roughjs rendering algorithm.
ext_fixtures/demos/flowchart/22.mmd	Stadium shape uses roughjs for path generation even with roughness=0; byte-exact parity requires porting roughjs rendering algorithm.
ext_fixtures/demos/flowchart/23.mmd	Stadium shape uses roughjs for path generation even with roughness=0; byte-exact parity requires porting roughjs rendering algorithm.
ext_fixtures/demos/flowchart/57.mmd	Stadium shape uses roughjs for path generation even with roughness=0; byte-exact parity requires porting roughjs rendering algorithm.
ext_fixtures/demos/flowchart/42.mmd	KaTeX math rendering ($$...$$); byte-exact parity requires porting KaTeX renderer output.
ext_fixtures/demos/flowchart/43.mmd	KaTeX math rendering ($$...$$); byte-exact parity requires porting KaTeX renderer output.
ext_fixtures/demos/flowchart/44.mmd	KaTeX math rendering ($$...$$); byte-exact parity requires porting KaTeX renderer output.
ext_fixtures/demos/flowchart/45.mmd	KaTeX math rendering ($$...$$); byte-exact parity requires porting KaTeX renderer output.
ext_fixtures/demos/flowchart/46.mmd	KaTeX math rendering ($$...$$); byte-exact parity requires porting KaTeX renderer output.
ext_fixtures/demos/flowchart/47.mmd	KaTeX math rendering ($$...$$); byte-exact parity requires porting KaTeX renderer output.
