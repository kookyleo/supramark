import type { ExampleDefinition } from '@supramark/core';

export const codeHighlightPresetDocsExamples: ExampleDefinition[] = [
  {
    name: 'Docs preset',
    description: 'Highlights common documentation and config snippets.',
    markdown: ['```json', '{ "name": "supramark" }', '```'].join('\n'),
  },
];
